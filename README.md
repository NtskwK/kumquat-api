## Launch with settings for development

You have to replace `/` or `\ ` with `.`!

```shell
python manage.py runserver --settings=path.to.setting.py
python manage.py runserver --settings kumquatAPI.settings_dev
```
